FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt --no-cache-dir

COPY src/ ./src/
EXPOSE 8000

CMD ["uvicorn", "src.rag_pbd:app", "--host", "0.0.0.0", "--port", "8000"]
